# v1_2008_probe_015

## Question

`What were the main causes of the 2008 financial crisis?`

## Exact command used

```bash
set -a && source .secrets && \
OPENAI_API_KEY=dummy \
ASAR_MODEL_PROVIDER=openai \
ASAR_MODEL_MODEL='llama3.1:8b' \
ASAR_OPENAI_BASE_URL='https://inference.ccrolabs.com/v1' \
ASAR_MODEL_MAX_TOKENS=512 \
ASAR_SEARCH_PROVIDER=tavily \
uv run python -m asar.demo \
  "What were the main causes of the 2008 financial crisis?" \
  --mode live \
  --output-dir experiments/runs/v1_2008_probe_015
```

## Artifact paths

- Output: [output.json](/home/tonystark/Desktop/multi-step-agent-research/experiments/runs/v1_2008_probe_015/20260319T224555Z_what-were-the-main-causes-of-the-2008-fi_experiment_f84bdc7bb2d3/output.json)
- Experiment: [experiment.json](/home/tonystark/Desktop/multi-step-agent-research/experiments/runs/v1_2008_probe_015/20260319T224555Z_what-were-the-main-causes-of-the-2008-fi_experiment_f84bdc7bb2d3/experiment.json)

## Result

### Final claims

1. `The deregulation of the financial system contributed to the 2008 financial crisis.`
2. `Securitization contributed to the 2008 financial crisis.`
3. `Monetary policy contributed to the 2008 financial crisis.`

### Metrics

- Evidence items: `9`
- Claim count: `3`
- Evidence utilization: `0.3333`
- Groundedness: `0.3333`
- Supported claims: `1`
- Verification summary: `supported=1, insufficient=2, contradicted=0`

## Comparison

### Versus v1_2008_probe_014

- `014` was much stronger overall.
- `014` had `groundedness=1.0` and `supported=3`; `015` fell to `groundedness=0.3333` and `supported=1`.
- `014` preserved the sharper OTC-derivatives mechanism; `015` lost it and reverted to the broader `deregulation of the financial system` claim.
- `014` kept a supported subprime-family mechanism; `015` replaced that with a much weaker generic `Monetary policy` claim.

### Versus the 2008 run inside v1_tier1_eval_set_003

- `015` recovered claim count from `2` to `3`.
- But it was clearly worse on support quality.
- `v1_003` had `groundedness=1.0` with `2` supported claims; `015` had `groundedness=0.3333` with only `1` supported claim.
- The full-set completeness failure mode changed shape rather than disappearing: instead of collapsing to `2` claims, `015` kept `3` claims but two were too weak to verify.

### Versus v0_2008_probe_006

- `006` remains much stronger.
- `006` had `3` supported direct-causal claims with `groundedness=1.0`.
- `015` matched `006` only on raw claim count, not on support quality or mechanism quality.
- `006` kept the sharper mix:
  - `Securitization...`
  - `Deregulation of the OTC derivatives market...`
  - `The failure of monetary policy...`
- `015` lost the OTC-derivatives claim and weakened overall support.

## Assessment

- Direct causal phrasing remained clean.
- Claim count remained healthy at `3`.
- OTC-derivatives did **not** stay preserved.
- The securitization family remained present, but not in a more stable or stronger-supported way than `014`.
- The mortgage-backed/subprime completeness hardening did **not** translate into a better live 2008 result here.

## Answers

- Did claim count remain at 3?
  - Yes.
- Did groundedness remain near 1.0?
  - No. It fell sharply to `0.3333`.
- Did OTC-derivatives remain present?
  - No.
- Was the securitization / mortgage-backed / subprime family represented in a supported way?
  - Only partially. `Securitization` remained, but verification marked it `insufficient`.
- Did the previous 2008 full-set completeness failure mode disappear?
  - No. The run avoided a `2`-claim collapse, but replaced it with a worse support-quality collapse.

## Bottom line

`v1_2008_probe_015` is a regression versus `v1_2008_probe_014` and does not support the claim that 2008 completeness/stability improved under live conditions.
